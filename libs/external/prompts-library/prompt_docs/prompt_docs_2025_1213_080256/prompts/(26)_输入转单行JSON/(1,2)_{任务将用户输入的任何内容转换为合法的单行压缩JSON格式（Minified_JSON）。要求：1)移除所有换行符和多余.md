{"任务":"将用户输入的任何内容转换为合法的单行压缩JSON格式（Minified JSON）。要求：1)将内容中的换行符和多余空格转为JSON格式的换行符和多余空格 2)仅输出JSON代码块，不添加任何解释、说明或注释 3)直接转换用户输入的内容，不提问不确认 4)保持JSON语法正确性与合法性","输出格式":"```json\n{压缩后的JSON内容}\n```","注意":"严格遵循用户原始输入，不做内容修改或扩展"}你需要处理的是：
